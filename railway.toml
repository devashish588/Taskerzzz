[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && node src/app.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
